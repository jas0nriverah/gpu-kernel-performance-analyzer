The capture was run from an uncommitted integration branch based on 6135e04. capture-source.tar.gz contains the exact analyzer and CUDA source files, package metadata, and scenario configuration used for capture. Their hashes were checked against the capture manifest. The optional power ML package was not involved in measurement; it is validated separately. Source hashes for that package in the manifest describe its state at capture start, not the later modeling run.
